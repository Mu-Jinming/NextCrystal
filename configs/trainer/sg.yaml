# path: configs/trainer/sg.yaml
accelerator: auto
devices: auto
strategy: auto
precision: 32-true
max_epochs: 60
gradient_clip_val: 1.0
accumulate_grad_batches: 2
deterministic: true
log_every_n_steps: 20