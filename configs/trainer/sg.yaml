# path: configs/trainer/sg.yaml
accelerator: auto
devices: auto
strategy: auto
precision: 16-mixed
max_epochs: 200
gradient_clip_val: 1.0
accumulate_grad_batches: 4
deterministic: true
log_every_n_steps: 20
